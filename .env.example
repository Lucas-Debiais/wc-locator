# Public Mapbox access token (pk.*) used to render the map.
# Create one at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=

# OpenDataSoft API key for the Nantes toilets dataset (read-only).
VITE_NANTES_API_KEY=
